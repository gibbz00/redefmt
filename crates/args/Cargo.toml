[package]
name = "redefmt-args"

authors.workspace = true
edition.workspace = true
exclude.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
version.workspace = true

[features]
builder = ["dep:bon"]
provided-args = ["dep:check_keyword", "dep:proc-macro2", "dep:syn"]
serde = ["dep:serde"]

[lints]
workspace = true

[dependencies]
derive-getters = "0.5"
foldhash = { version = "0.1.5", default-features = false }
hashbrown = { version = "0.15", default-features = false, features = ["default-hasher"] }
indexmap = { version = "2.9", default-features = false }
thiserror = { version = "2.0", default-features = false }
unicode-xid = "0.2"

# feature: builder
bon = { version = "3.6", default-features = false, optional = true }

# feature: serde
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"], optional = true }

# feature: provided-args
check_keyword = { version = "0.4", optional = true }
proc-macro2 = { version = "1.0", optional = true }
syn = { version = "2.0", default-features = false, features = ["extra-traits", "parsing", "printing"], optional = true }

[dev-dependencies]
redefmt-args = { path = ".", features = ["builder", "provided-args", "serde"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
